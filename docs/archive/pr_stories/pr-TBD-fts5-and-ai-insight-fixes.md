- Fixed backend 500 error during FTS5 `search` by catching `sqlite3.OperationalError` inside `VerseRepo.search_text` and falling back safely to a literal string search query parameter when syntax characters like colons `:` break FTS schema (src/clible/db/repositories/verse_repo.py).
- Added `test_search_text_handles_invalid_fts5_syntax` test case to verify FTS5 syntax auto-escaping works natively without regressions (tests/test_db/test_repositories/test_verse_repo.py).
- Resolved web transition bug: clicking an FTS5 search result card now successfully pushes the UI into 'verse' mode lookup instead of forcing another FTS5 text search against the raw reference target (src/clible-web/App.tsx).
- Surfaced detailed API exception streams inside `getAiInsight` and `getAiTone` (`details?.details`) directly to the UI, clarifying opaque user 500 crashes returning from downstream `@google/genai` model exceptions (src/clible-web/services/bibleService.ts).
- Upgraded hardcoded `#333` and `#1A1A1A` text colors within the reader view's markdown renderer, syncing them with global theme tokens (`var(--text)`, `var(--border-soft)`) for readability in dark mode (src/clible-web/utils/markdownComponents.tsx).
