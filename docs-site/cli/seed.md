# `clible seed`

Manage Bible translations: list what is available, install, list installed, or remove.

```bash
clible seed available
clible seed install <id>
clible seed list
clible seed remove <id>
```

Example:

```bash
uv run clible seed install web
```

