---
description: PR description format — when writing PR_STORY.md or pull request body for clible
alwaysApply: false
---

# PR stories (PR description format)

**Templates:** Short and Long formats are in `.cursor/rules/pr-templates.md`.

**Tracked storage:** For each PR, add a file under **`pr_stories/`** (version controlled). Follow naming in `pr_stories/README.md`. Paste the same content into the GitHub PR description. Short Format for routine PRs; Long Format for releases or complex changes.

**Local draft:** `PR_STORY.md` at the repo root remains gitignored for scratch work; prefer committing the final text under `pr_stories/`.

When drafting a PR description or updating **PR_STORY.md**, you may use the structure below. Copy the content into the PR body (e.g. on GitHub).

## Structure

1. **Title (first line)**  
   Conventional commit + short summary, e.g.:  
   `# feat: support verse ranges in verse command (multi-verse lookup)`

2. **Intro paragraph**  
   One or two sentences: what the PR does and what it builds on (e.g. previous PR or ticket).

3. **## Summary**  
   Bullet list of main changes (parsing, repos, services, CLI). One line per area; technical but concise.

4. **## Files added**  
   New files only, with a one-line note per file (e.g. tests for X).

5. **## Files modified**  
   Changed files with a short note on what changed in each.

6. **## Tests**  
   How to run the relevant tests and the full suite. Include test count and that they pass, e.g.:  
   `uv run pytest -v` — **N tests**, all passing.

7. **## Usage** (optional)  
   Example commands or steps for the new/updated behavior (code block with `bash` or similar).

Keep the whole document short and scannable. Use it to review the PR yourself and to give reviewers a clear picture without reading every diff.
